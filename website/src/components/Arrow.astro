---
interface Props { direction?: 'right' | 'down'; }
const { direction = 'right' } = Astro.props;
---
<svg class:list={['arrow-icon', { down: direction === 'down' }]} viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M4 12h15M13 5l7 7-7 7" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" /></svg>
